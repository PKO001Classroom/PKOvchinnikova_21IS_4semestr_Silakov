# Аналитический журнал знаний

Desktop-приложение для систематизации учебных конспектов с продвинутой аналитикой активности пользователя и автоматическим формированием отчётов.

## Функциональность

### 📝 Управление конспектами
- Создание и редактирование конспектов в формате Markdown
- Категоризация конспектов по темам
- Автоматическое создание структурированных шаблонов
- Интегрированный редактор с подсветкой синтаксиса Markdown

### 🏷️ Тегирование и организация
- Гибкая система тегов для классификации конспектов
- Просмотр статистики по использованию тегов
- Поиск и фильтрация конспектов по тегам и категориям
- Массовое управление тегами

### 📊 Аналитика и статистика
- **Автоматическое отслеживание активности**:
  - Логирование создания, обновления и просмотра конспектов
  - Статистика активности по дням и неделям
  - Анализ популярности категорий и тегов
- **Визуализация данных**:
  - Графики активности в Excel отчётах
  - Диаграммы распределения конспектов по категориям
  - Отслеживание прогресса обучения

### 📈 Экспорт и отчёты
- **Excel отчёты**:
  - Автоматическое создание таблиц с данными
  - Встроенные графики и диаграммы
  - Статистика по всем метрикам
- **PDF отчёты**:
  - Профессиональное оформление
  - Структурированные таблицы данных
  - Автоматическое форматирование
- Экспорт в удобные форматы для презентаций и анализа

### 🔍 Поиск и навигация
- Быстрый поиск по названиям конспектов
- Фильтрация по категориям и тегам
- Просмотр недавно обновлённых конспектов
- Сортировка по различным критериям

## Технологии

- **Python 3.8+** – основной язык программирования
- **PostgreSQL** – реляционная база данных для хранения конспектов и метаданных
- **Tkinter** – графический интерфейс пользователя
- **psycopg2** – адаптер для работы с PostgreSQL
- **openpyxl** – генерация Excel отчётов с графиками
- **reportlab** – создание PDF документов
- **Markdown** – формат хранения конспектов
- **SQL** – запросы и аналитика данных

## Структура проекта

```
analytical_knowledge_journal/
├── main.py                    # Точка входа приложения
├── requirements.txt           # Зависимости проекта
├── config.py                  # Конфигурация приложения
├── test_database.py          # Тестирование функционала
├── README.md                  # Документация
│
├── src/                      # Исходный код приложения
│   ├── database.py           # Менеджер работы с БД
│   ├── file_manager.py       # Управление файлами конспектов
│   ├── gui.py                # Графический интерфейс
│   └── reporting.py          # Генерация отчётов
│
├── notes_md/                 # Папка для конспектов (Markdown)
│   ├── 20260127_125555_21.01.2026.md
│   └── ... (другие конспекты)
│
├── exports/                  # Папка для экспортированных отчётов
│   ├── report_20260127_131351.xlsx
│   ├── report_20260127_131351.pdf
│   └── ... (другие отчёты)
│
└── competencies.json         # Настройки компетенций (опционально)
```

## Установка и настройка

### 1. Предварительные требования

1. **Установите PostgreSQL:**
   - Windows: [PostgreSQL для Windows](https://www.postgresql.org/download/windows/)
   - macOS: `brew install postgresql`
   - Linux: `sudo apt-get install postgresql postgresql-contrib`

2. **Создайте базу данных:**
   ```sql
   CREATE DATABASE knowledge_journal;
   CREATE USER journal_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE knowledge_journal TO journal_user;
   ```

3. **Создайте необходимые таблицы:**
   ```sql
   CREATE TABLE notes (
       id SERIAL PRIMARY KEY,
       title VARCHAR(255) NOT NULL,
       file_path TEXT NOT NULL,
       created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
       updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
       category VARCHAR(100)
   );

   CREATE TABLE tags (
       id SERIAL PRIMARY KEY,
       name VARCHAR(50) UNIQUE NOT NULL
   );

   CREATE TABLE note_tags (
       note_id INTEGER REFERENCES notes(id) ON DELETE CASCADE,
       tag_id INTEGER REFERENCES tags(id) ON DELETE CASCADE,
       PRIMARY KEY (note_id, tag_id)
   );

   CREATE TABLE activity_log (
       id SERIAL PRIMARY KEY,
       note_id INTEGER REFERENCES notes(id) ON DELETE SET NULL,
       event_type VARCHAR(20) NOT NULL,
       event_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
   );
   ```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

**requirements.txt содержит:**
```
psycopg2-binary==2.9.9        # PostgreSQL адаптер
openpyxl==3.1.2               # Excel отчёты
reportlab==4.0.4              # PDF отчёты
matplotlib==3.7.1             # Графики (опционально)
Pillow==10.0.0                # Обработка изображений
pandas==2.0.3                 # Анализ данных
```

### 3. Настройка конфигурации

Отредактируйте файл `config.py`:
```python
DB_CONFIG = {
    'dbname': 'knowledge_journal',    # Имя вашей базы данных
    'user': 'journal_user',           # Имя пользователя
    'password': 'your_password',      # Ваш пароль
    'host': 'localhost',              # Хост БД
    'port': '5432'                    # Порт PostgreSQL
}
```

### 4. Запуск приложения

```bash
python main.py
```

## Использование

### 1. Создание конспектов
- Перейдите на вкладку "📝 Конспекты"
- Введите название и категорию
- Нажмите "Создать" – автоматически создастся файл с шаблоном
- Редактируйте конспект в текстовом редакторе с поддержкой Markdown
- Используйте горячие клавиши для форматирования

### 2. Управление тегами
- Выберите конспект из списка
- Введите тег в поле "Новый тег" и нажмите "Добавить"
- Просматривайте все теги конспекта в специальной панели
- Удаляйте ненужные теги кнопкой "Удалить"

### 3. Анализ активности
- Перейдите на вкладку "📊 Аналитика"
- Просматривайте статистику:
  - Общее количество конспектов и тегов
  - Активность за сегодня/неделю/месяц
  - Распределение по категориям
  - Популярные теги
- Используйте кнопки обновления для актуальных данных

### 4. Генерация отчётов
- На вкладке "📊 Аналитика" нажмите:
  - "📈 Сформировать Excel отчёт" – создаст файл с графиками
  - "📄 Сформировать PDF отчёт" – создаст документ в PDF формате
- Отчёты автоматически сохраняются в папку `exports/`
- Отчёты содержат дату создания в названии файла

### 5. Поиск и фильтрация
- Используйте сортировку в списке конспектов
- Фильтруйте по категориям через статистику
- Ищите по тегам на вкладке "🏷️ Теги"
- Просматривайте последние обновления

## Особенности

### 🎯 Уникальные возможности:
- **Автоматическое логирование** – каждое действие пользователя отслеживается для аналитики
- **Двойное хранение** – конспекты в файлах, метаданные в БД
- **Интеграция с Markdown** – знакомый синтаксис для академических записей
- **Профессиональные отчёты** – готовые для презентаций и отчётов
- **Масштабируемость** – PostgreSQL обеспечивает работу с большими объёмами данных

### 🔧 Технические преимущества:
- **Модульная архитектура** – легко расширять функционал
- **Кроссплатформенность** – работает на Windows, macOS, Linux
- **Автономность** – не требует интернета после установки
- **Резервное копирование** – экспорт в стандартные форматы
- **Безопасность** – локальное хранение данных

### 📊 Аналитические функции:
- **Тренды обучения** – отслеживание прогресса во времени
- **Распределение внимания** – анализ по категориям и тегам
- **Эффективность** – оценка продуктивности по дням/неделям
- **Качество конспектов** – статистика по обновлениям

## Структура базы данных

```
┌─────────────┐      ┌─────────────┐      ┌─────────────┐
│    notes    │      │  note_tags  │      │    tags     │
├─────────────┤      ├─────────────┤      ├─────────────┤
│ id          │◄─────┤ note_id     │      │ id          │
│ title       │      │ tag_id      │─────►│ name        │
│ file_path   │      └─────────────┘      └─────────────┘
│ created_at  │
│ updated_at  │      ┌─────────────┐
│ category    │      │activity_log │
└─────────────┘      ├─────────────┤
               ┌─────┤ note_id     │
               │     │ event_type  │
               │     │ event_time  │
               │     └─────────────┘
               │
               └───── Связь через Foreign Key
```

## Примеры использования

### Для студентов:
- Ведение конспектов по всем предметам
- Подготовка к экзаменам с системой тегов
- Отслеживание прогресса обучения
- Создание отчётов для кураторов

### Для преподавателей:
- Систематизация учебных материалов
- Анализ популярности тем
- Подготовка методических материалов
- Создание отчётов по активности студентов

### Для исследователей:
- Организация исследовательских заметок
- Тематическая классификация
- Анализ времени, затраченного на темы
- Экспорт данных для научных публикаций

## Устранение неполадок

### 1. Ошибка подключения к PostgreSQL:
```bash
# Проверьте статус PostgreSQL
sudo systemctl status postgresql

# Проверьте конфигурацию
sudo -u postgres psql -c "\l"
```

### 2. Проблемы с зависимостями:
```bash
# Обновление pip
pip install --upgrade pip

# Переустановка зависимостей
pip uninstall -r requirements.txt -y
pip install -r requirements.txt
```

### 3. Ошибки генерации отчётов:
```bash
# Установка недостающих библиотек
pip install matplotlib pillow
```

### 4. Проблемы с правами доступа:
```bash
# Проверьте права на папки
chmod 755 notes_md exports
```

## Лицензия

Проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## Поддержка и развитие

- **Отчёты об ошибках**: [GitHub Issues](#)
- **Предложения**: [Discussions](#)
- **Документация**: [Wiki](#)

---
