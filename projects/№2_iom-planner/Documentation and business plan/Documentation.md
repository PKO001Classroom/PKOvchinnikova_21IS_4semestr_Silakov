## 📋 ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ

### 1. **Назначение системы**
Приложение помогает студентам/сотрудникам планировать, отслеживать и анализировать свой образовательный маршрут:
- Постановка и управление учебными целями
- Отслеживание развития компетенций и навыков
- Автоматический расчёт прогресса и достижений
- Формирование профессионального портфолио в виде Word-отчётов

### 2. **Архитектура и технологии**
- **Язык**: Python 3.7+
- **GUI**: Tkinter (стандартная библиотека)
- **База данных**: SQLite (встроенная, файл `iom.db`)
- **Зависимости**:
  - `python-docx` – генерация Word-документов
  - `tkinter` – графический интерфейс (обычно входит в поставку Python)
- **Структура данных**: 7 связанных таблиц (цели, навыки, компетенции, достижения и др.)

### 3. **Основные функции (модули)**
#### **Модуль 1: Управление целями**
- CRUD-операции (создание, чтение, обновление, удаление)
- Расширенные поля: тип, статус, даты, темп, описание с упрощённой разметкой
- Привязка навыков (до 3) и компетенций (1-3 с уровнями 1-5)

#### **Модуль 2: Профиль и статистика**
- Визуализация навыков (график/таблица)
- Статистика по типам целей (завершено/всего)
- Расчёт процента целей, завершённых в срок

#### **Модуль 3: Система компетенций**
- Загрузка компетенций из JSON-файла
- Расчёт среднего уровня по завершённым целям
- Автоматическое определение "слабых зон" (уровень < 3)
- Персонализированные рекомендации для развития

#### **Модуль 4: Геймификация (достижения)**
- 5 автоматически проверяемых достижений:
  1. **Старт** – создана первая цель
  2. **Пунктуальный** – 3+ целей завершены в срок
  3. **Многогранный** – цели 3+ разных типов
  4. **Навыковый рост** – 4+ цели с одним навыком
  5. **Планирование** – 5+ целей "В процессе" одновременно

#### **Модуль 5: Семестровое планирование**
- Отдельные цели на семестр (например, "пройти 3 курса")
- Автоматический расчёт прогресса на основе завершённых целей
- Ручное добавление/удаление семестровых целей

#### **Модуль 6: Отчётность**
- Экспорт всего ИОМ в структурированный Word-документ
- Сохранение с датой в имени файла
- Форматирование: заголовки, списки, таблицы, гиперссылки

#### **Модуль 7: Настройки**
- Выбор специальности (влияет на компетенции)
- Очистка всех данных (тестовый режим)
- Информация о программе

### 4. **Структура базы данных**
```sql
цели (id, название, тип, статус, план_дата, факт_дата, темп, описание)
навыка (id, название) 
цель_навыки (цель_id, навык_id)
компетенции (id, название, категория)
цель_компетенции (цель_id, компетенция_id, уровень)
достижения (код, название, описание, получено)
цель_каса (id, текст_цели, тип_цели, параметр, текущий_прогресс, целевой_прогресс)
```

### 5. **Особенности реализации**
- **Автономность**: Не требует интернета, всё локально
- **Простая разметка**: Псевдо-Markdown в описаниях целей
- **Автоматические обновления**: Прогресс пересчитывается при любых изменениях
- **Валидация**: Проверка дат, ограничение навыков/компетенций
- **Резервные копии**: Файлы отчётов сохраняются с timestamp

### 6. **Требования к установке**
```bash
# 1. Установка Python 3.7+
# 2. Установка зависимостей
pip install python-docx

# 3. Запуск
python main.py

# 4. Обязательные файлы:
# - main.py (основная программа)
# - competencies.json (справочник компетенций)
# - iom.db (создаётся автоматически)
```
