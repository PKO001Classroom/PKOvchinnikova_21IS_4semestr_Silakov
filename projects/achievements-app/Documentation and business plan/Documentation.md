## 📄 ДОКУМЕНТАЦИЯ

### 1. Общее описание проекта
Проект "Учёт личных учебных достижений" — это десктопное приложение на Python для систематизации и управления информацией об учебных, научных и творческих достижениях пользователя.

### 2. Технологический стек
- **Язык программирования**: Python 3.x
- **Графический интерфейс**: Tkinter
- **База данных**: SQLite3
- **Работа с документами**: python-docx
- **Обработка данных**: JSON

### 3. Структура проекта

#### Файлы:
- **main.py** — основной файл приложения
- **types.json** — конфигурационный файл с типами достижений
- **достижения.db** — база данных SQLite (создаётся автоматически)
- **достижения.docx** — экспортируемый документ Word (создаётся при экспорте)

#### Основные модули:
1. **Инициализация БД** (`init_db()`)
2. **Загрузка типов** (`load_types()`)
3. **Работа с базой данных** (CRUD-операции)
4. **Экспорт в Word** (`export_to_word()`)
5. **Графический интерфейс** (класс `AchievementsApp`)

### 4. Функциональные возможности

#### 4.1. Основные функции:
- ✅ Добавление новых достижений
- ✅ Просмотр списка всех достижений
- ✅ Детальный просмотр информации
- ✅ Удаление записей
- ✅ Экспорт в формат Word
- ✅ Фильтрация по типам и уровням

#### 4.2. Поля для каждого достижения:
- Название достижения (обязательное)
- Дата в формате ГГГГ-ММ-ДД (обязательное)
- Тип (выбор из списка)
- Уровень (школьный, городской и т.д.)
- Описание (текстовое поле)

### 5. Установка и запуск

#### Требования:
```bash
pip install tkinter sqlite3 python-docx
```

#### Запуск:
```bash
python main.py
```

### 6. База данных

#### Структура таблицы `достижения`:
```sql
CREATE TABLE достижения (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    название TEXT NOT NULL,
    дата TEXT NOT NULL,
    тип TEXT,
    уровень TEXT,
    описание TEXT
)
```

### 7. Графический интерфейс

#### Вкладки:
1. **"Добавить достижение"** — форма для ввода данных
2. **"Мои достижения"** — список всех записей с управлением

#### Элементы управления:
- Поля ввода (текстовые)
- Выпадающие списки (Combobox)
- Многострочное текстовое поле
- Кнопки действий
- Прокручиваемый список
- Статусная строка

### 8. Обработка ошибок
- Валидация обязательных полей
- Обработка исключений при работе с БД
- Проверка существования файлов
- Информативные сообщения об ошибках

### 9. Форматы данных

#### 9.1. JSON-конфигурация (types.json):
```json
[
    "Олимпиада",
    "Сертификат",
    "Проект",
    "Экзамен",
    "Конференция"
]
```

#### 9.2. Экспорт в Word:
- Заголовок документа
- Форматированный список достижений
- Автоматическое сохранение в файл `достижения.docx`

### 10. Расширяемость
- Легко добавлять новые типы достижений через JSON-файл
- Возможность добавления новых полей в БД
- Модульная архитектура для добавления новых функций
