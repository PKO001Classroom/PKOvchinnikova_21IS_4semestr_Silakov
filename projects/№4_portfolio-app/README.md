```markdown
# Электронный портфолио студента-исследователя

Desktop-приложение для систематизации учебных, научных и исследовательских достижений студента с возможностью генерации отчетов.

## 📋 Функциональность

- **Управление записями**: Создание, редактирование, удаление записей о достижениях
- **Категоризация**: Разделение записей по типам (публикации, конференции, гранты, преподавание, достижения)
- **Работа с описаниями**: Редактирование в формате Markdown с подсказками по синтаксису
- **Система соавторов**: Добавление и управление соавторами для записей
- **Генерация отчетов**:
  - Excel отчеты с графиками
  - Word отчеты с форматированием
  - Текстовые отчеты (гарантированно работают)
- **Поиск и фильтрация**: Удобный просмотр всех записей в табличном виде
- **Статистика**: Анализ активности и достижений

## 🛠 Технологии

- **Python 3.8+** - основной язык программирования
- **Tkinter** - графический интерфейс
- **PostgreSQL** - база данных
- **psycopg2** - драйвер для работы с PostgreSQL
- **openpyxl** - создание Excel файлов
- **python-docx** - создание Word документов
- **Markdown** - формат описаний записей

## 📁 Структура проекта

```
portfolio_project/
├── gui.py              # Основной графический интерфейс
├── database.py         # Работа с базой данных
├── file_handler.py     # Управление файлами (.md)
├── exporter.py         # Генерация отчетов (Excel, Word, текстовых)
├── requirements.txt    # Зависимости
├── README.md          # Документация
├── portfolio_md/       # Папка с Markdown файлами
├── reports/           # Папка с отчетами
└── screenshots/       # Папка для скриншотов
```

## 🚀 Установка и запуск

### Предварительные требования

1. Установите **PostgreSQL** и создайте базу данных:
   ```sql
   CREATE DATABASE research_portfolio;
   ```

2. Создайте необходимые таблицы (SQL скрипты приведены ниже)

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Настройка базы данных

Отредактируйте файл `database.py` для настройки подключения:

```python
self.conn = psycopg2.connect(
    host="localhost",
    database="research_portfolio",
    user="postgres",
    password="ваш_пароль"  # Замените на свой пароль
)
```

### SQL для создания таблиц

```sql
-- Таблица записей
CREATE TABLE entries (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    entry_type VARCHAR(50) NOT NULL,
    year INTEGER,
    file_path TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица соавторов
CREATE TABLE coauthors (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100) NOT NULL UNIQUE
);

-- Таблица связи записей и соавторов
CREATE TABLE entry_coauthors (
    entry_id INTEGER REFERENCES entries(id) ON DELETE CASCADE,
    coauthor_id INTEGER REFERENCES coauthors(id) ON DELETE CASCADE,
    PRIMARY KEY (entry_id, coauthor_id)
);

-- Таблица логов активности (опционально)
CREATE TABLE activity_log (
    id SERIAL PRIMARY KEY,
    entry_id INTEGER REFERENCES entries(id),
    event_type VARCHAR(20),
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Запуск приложения

```bash
python gui.py
```

## 📊 Использование

### Основные функции

1. **Создание записи**:
   - Введите название, выберите тип, укажите год
   - Напишите описание в формате Markdown
   - Нажмите "Создать"

2. **Редактирование записи**:
   - Выберите запись из таблицы
   - Внесите изменения в поля
   - Нажмите "Сохранить"

3. **Добавление соавторов**:
   - Выберите запись
   - Введите имя соавтора в поле "Добавить соавтора"
   - Нажмите "Добавить"

4. **Генерация отчетов**:
   - Excel отчет: содержит статистику и графики
   - Word отчет: оформленный документ с разделами
   - Текстовый отчет: простой и надежный вариант

### Типы записей

- **Публикация** - научные статьи, публикации
- **Конференция** - участие в конференциях
- **Грант** - полученные гранты и финансирование
- **Преподавание** - педагогическая деятельность
- **Достижение** - награды, сертификаты, дипломы

## 🔧 Требования

- Python 3.8 или выше
- PostgreSQL 12+
- Минимум 2 ГБ оперативной памяти
- 500 МБ свободного места на диске

## 📄 Форматы файлов

- **Записи**: Сохраняются как .md файлы в папке `portfolio_md/`
- **Отчеты Excel**: .xlsx файлы в папке `reports/`
- **Отчеты Word**: .docx файлы в папке `reports/`
- **Текстовые отчеты**: .txt файлы в папке `reports/`

## 🎯 Особенности

- **Кроссплатформенность**: Работает на Windows, macOS, Linux
- **Автосохранение**: Все изменения сохраняются автоматически
- **Резервное копирование**: Markdown файлы можно редактировать отдельно
- **Расширяемость**: Легко добавить новые типы записей и форматы отчетов
- **Локализация**: Поддержка русского языка

## 📈 Статистика и аналитика

Приложение автоматически собирает статистику:
- Количество записей по типам
- Распределение по годам
- Количество уникальных соавторов
- Активность пользователя (создание, редактирование)

## 🆘 Поддержка

При возникновении проблем:

1. Проверьте подключение к базе данных
2. Убедитесь, что все зависимости установлены
3. Проверьте права доступа к папкам `reports/` и `portfolio_md/`
4. Убедитесь, что PostgreSQL запущен

Для получения помощи создайте issue в репозитории проекта.

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл LICENSE для подробностей.

## 🤝 Вклад в проект

Если вы хотите внести свой вклад:
1. Форкните репозиторий
2. Создайте ветку для вашей функции
3. Внесите изменения
4. Создайте Pull Request

---

**Версия**: 1.0.0  
**Дата последнего обновления**: 28.01.2026  
**Автор**: Команда разработчиков "Электронный портфолио"  
**Контакты**: portfolio.support@example.com
```

Этот README.md содержит все основные элементы из скриншота: описание проекта, функциональность, технологии, инструкции по установке, использование, требования и другую важную информацию. Он структурирован аналогично представленному в задании примеру.
