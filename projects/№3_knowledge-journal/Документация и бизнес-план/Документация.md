## 📋 ТЕХНИЧЕСКАЯ ДОКУМЕНТАЦИЯ

### 1. **Архитектура проекта**

```
analytical_knowledge_journal/
├── main.py                 # Точка входа
├── config.py              # Конфигурация
├── requirements.txt       # Зависимости
├── README.md             # Документация
├── test_database.py      # Тесты
├── notes_md/            # Папка с конспектами (.md)
├── exports/             # Папка с отчётами (PDF/Excel)
└── src/                 # Исходный код
    ├── database.py      # Менеджер БД
    ├── file_manager.py  # Работа с файлами
    ├── gui.py           # Графический интерфейс
    └── reporting.py     # Генерация отчётов
```

### 2. **Стек технологий**
- **Язык**: Python 3.8+
- **GUI**: Tkinter
- **База данных**: PostgreSQL
- **Формат конспектов**: Markdown (.md)
- **Отчётность**: 
  - Excel: openpyxl + matplotlib
  - PDF: reportlab
- **Дополнительно**: pandas, Pillow

### 3. **Структура базы данных**
```sql
-- Основные таблицы:
notes (id, title, category, file_path, created_at, updated_at)
tags (id, name)
note_tags (note_id, tag_id)
activity_log (id, note_id, event_type, event_time)

-- Типы событий (activity_log):
'CREATE'  - создание конспекта
'UPDATE'  - редактирование
'VIEW'    - просмотр
```

### 4. **Основные модули**

#### **4.1. DatabaseManager (`database.py`)**
- CRUD-операции для конспектов
- Управление тегами (многие-ко-многим)
- Логирование активности (создание, просмотр, редактирование)
- Статистика:
  - Конспекты по категориям
  - Топ тегов
  - Активность по дням
  - Общая статистика

#### **4.2. FileManager (`file_manager.py`)**
- Создание/чтение/удаление MD-файлов
- Автоматическое именование: `YYYYMMDD_HHMMSS_Название.md`
- Шаблоны конспектов
- Открытие файлов во внешнем редакторе

#### **4.3. KnowledgeJournalGUI (`gui.py`)**
**Три основные вкладки:**

1. **📝 Конспекты** (основная)
   - Список конспектов с фильтрацией
   - Редактор Markdown с подсветкой синтаксиса
   - Управление тегами
   - Предпросмотр

2. **📊 Аналитика**
   - Визуализация статистики
   - Графики активности
   - Экспорт в Excel/PDF
   - Топ тегов и категорий

3. **🏷️ Теги**
   - Облако тегов
   - Статистика использования
   - Управление связями

#### **4.4. ReportGenerator (`reporting.py`)**
- **Excel отчёты**:
  - Диаграммы (BarChart)
  - Сводные таблицы
  - Цветовое оформление
- **PDF отчёты**:
  - Профессиональное форматирование
  - Таблицы с оформлением
  - Автоматическая разбивка на страницы

### 5. **Функциональные возможности**

#### **5.1. Управление конспектами**
- Создание с автоматическим шаблоном
- Редактирование в реальном времени
- Категоризация
- Полнотекстовый поиск (планируется)

#### **5.2. Система тегов**
- Неограниченное количество тегов
- Автоматическое удаление дубликатов
- Статистика популярности
- Фильтрация по тегам

#### **5.3. Аналитика и отчётность**
- **В реальном времени**:
  - Количество конспектов/тегов
  - Активность сегодня
  - Распределение по категориям
- **Экспортные отчёты**:
  - Еженедельная активность
  - Популярные теги
  - Последние изменения

#### **5.4. Особенности реализации**
- **Автоматическое логирование**: Каждое действие фиксируется
- **Оффлайн-работа**: Все данные локально
- **Резервное копирование**: MD-файлы + БД
- **Кроссплатформенность**: Windows/macOS/Linux

### 6. **Требования к установке**

```bash
# 1. Установка PostgreSQL
# Создание базы данных:
# CREATE DATABASE knowledge_journal;

# 2. Установка Python-зависимостей
pip install -r requirements.txt

# 3. Настройка конфигурации (config.py)
# Измените пароль в DB_CONFIG

# 4. Запуск приложения
python main.py
```

### 7. **SQL-скрипт для создания таблиц**
```sql
-- Таблица конспектов
CREATE TABLE notes (
    id SERIAL PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    category VARCHAR(100),
    file_path TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Таблица тегов
CREATE TABLE tags (
    id SERIAL PRIMARY KEY,
    name VARCHAR(50) UNIQUE NOT NULL
);

-- Связующая таблица
CREATE TABLE note_tags (
    note_id INTEGER REFERENCES notes(id) ON DELETE CASCADE,
    tag_id INTEGER REFERENCES tags(id) ON DELETE CASCADE,
    PRIMARY KEY (note_id, tag_id)
);

-- Логи активности
CREATE TABLE activity_log (
    id SERIAL PRIMARY KEY,
    note_id INTEGER REFERENCES notes(id) ON DELETE CASCADE,
    event_type VARCHAR(10) CHECK (event_type IN ('CREATE', 'UPDATE', 'VIEW')),
    event_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Индексы для производительности
CREATE INDEX idx_notes_category ON notes(category);
CREATE INDEX idx_activity_time ON activity_log(event_time);
CREATE INDEX idx_note_tags_note ON note_tags(note_id);
```
